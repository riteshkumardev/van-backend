export class Vehicle {
    constructor(id, plateNumber, brand, model, year, categoryId, ownerId) {
      this.id = id;
      this.plateNumber = plateNumber;
      this.brand = brand;
      this.model = model;
      this.year = year;
      this.categoryId = categoryId;
      this.ownerId = ownerId;
    }
  }
  